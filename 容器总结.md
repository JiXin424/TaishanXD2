# TaishanXD V2 容器状态总结

## 全部容器

| 容器名 | 镜像 | 服务 | 健康检查 | 宿主机端口 | 容器端口 |
|---|---|---|---|---|---|
| taishan-postgres | postgres:16-alpine | PostgreSQL 16 | healthy | 5433 | 5432 |
| taishan-redis | redis:7-alpine | Redis 7 | healthy | 6379 | 6379 |
| taishan-server | taishanfuneng2-server | Go (Gin) 后端 | healthy | 4007 | 4007 |
| taishan-web | taishanfuneng2-web | Next.js 前端 | running | 3000 | 3000 |
| taishan-traefik | traefik:v3.3 | 网关/路由 | running | 80, 8081 | 80, 8080 |

## 启动顺序与依赖

```
postgres (healthy) ─┐
                    ├─→ server (healthy) ─→ web
redis (healthy) ────┘
                    └─→ traefik
```

## 对外访问地址

| 入口 | 地址 | 说明 |
|---|---|---|
| 前端页面 | http://localhost:3000 | 登录页 → 仪表盘 |
| 后端 API | http://localhost:4007/api/health | Go 服务健康检查 |
| Traefik 网关 | http://localhost:80 | HTTP 统一入口 |
| Traefik Dashboard | http://localhost:8081 | 路由管理面板 |
| PostgreSQL | localhost:5433 | 数据库（用户 taishan） |
| Redis | localhost:6379 | 缓存（密码 taishan_dev） |

## 常用操作

```bash
# 全栈启动
docker compose up -d

# 全栈停止
docker compose down

# 重新构建并启动（代码修改后）
docker compose build && docker compose up -d

# 仅重建后端
docker compose build server && docker compose up -d server

# 仅重建前端
docker compose build web && docker compose up -d web

# 查看日志
docker compose logs -f server
docker compose logs -f web

# 进入数据库
docker exec -it taishan-postgres psql -U taishan -d taishan
```

## Demo 登录凭据

- 用户名: `admin`
- 密码: `admin123`

## 生产部署注意事项

- 移除 postgres/redis 的 `ports` 暴露，仅保留容器内部通信
- 修改 `.env.local` 中的 `SESSION_SECRET`、数据库密码等敏感配置
- 将 `docker-compose.yml` 中 `server` 的 `FRONTEND_URL` 改为生产域名
- 配置 Traefik HTTPS (Let's Encrypt) 替代 HTTP
